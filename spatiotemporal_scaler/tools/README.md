generate_articles.js
--------------------

使い方:

1. Node.js がインストールされていることを確認してください。
2. このスクリプトを実行すると `content/articles` に 189 個のプレースホルダ記事が生成されます。

実行:

```bash
node tools/generate_articles.js
```

既存ファイルは上書きされます。生成後、必要な記事を手動で編集してください。
